/* Coastal Modernist Beaches page: a coast chapter built from tide lines, river mouths, and room to slow down. */
import StoryPage, { storyConfigs } from "@/components/StoryPage";

export default function Beaches() {
  return <StoryPage config={storyConfigs.beaches} />;
}
