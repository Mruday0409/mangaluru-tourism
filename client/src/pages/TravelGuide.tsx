/* Coastal Modernist Travel Guide page: practical information in a calm, back-pocket format that keeps the itinerary open. */
import StoryPage, { storyConfigs } from "@/components/StoryPage";

export default function TravelGuide() {
  return <StoryPage config={storyConfigs.travel} />;
}
