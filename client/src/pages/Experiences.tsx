/* Coastal Modernist Experiences page: a loose day plan where the detour is part of the destination. */
import StoryPage, { storyConfigs } from "@/components/StoryPage";

export default function Experiences() {
  return <StoryPage config={storyConfigs.experiences} />;
}
