/* Coastal Modernist Culture page: living heritage, painted ceilings, laterite, and the old quarter in a slower register. */
import StoryPage, { storyConfigs } from "@/components/StoryPage";

export default function Culture() {
  return <StoryPage config={storyConfigs.culture} />;
}
